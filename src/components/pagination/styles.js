import styled from 'styled-components';
import { Link } from 'gatsby';

export const PaginationWrap = styled.aside`
  display: flex;
  justify-content: space-between;
`;

export const ActionLinks = styled(Link)``;

export const TextPages = styled.p``;
